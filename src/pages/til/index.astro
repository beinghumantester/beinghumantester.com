---
import BaseListing from "../../layouts/BaseListing.astro";
import { PAGES } from "../../config";

if (PAGES.til.isActive === false) {
    return Astro.redirect("/404");
}
---

<BaseListing
    collection="til"
    title={PAGES.til.title}
    subtitle={PAGES.til.subtitle}
/>
